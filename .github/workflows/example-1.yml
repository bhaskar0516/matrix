name: Matrix flow in CI-Pipeline
on: 
  push:
  workflow_dispatch:
jobs:
  deploy-job:
    strategy:
      matrix:
        os: [ubuntu-latest,windows-latest]
        images: [hello-world]
    runs-on: ${{ matrix.os}}
    steps:
      - name: Docker info
        run: docker info
      
      - name: Runs image on ${{ matrix.os}}
        run: docker run ${ matrix.images }}